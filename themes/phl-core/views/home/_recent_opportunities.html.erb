<% if @recent_opportunities.present? %>
  <div>
    <p><%= t('.background') %></p>
    <p><a href='<%= new_user_registration_path %>'><%= t('.cta') %></a></p>
  </div>
  <div class='recent_opps'>
    <h2><%= t('.title') %></h2>
    <!-- Inline style to correct issue with phl alpha pattern not looking great here - KBC -->
    <ul style="margin-left: 0;padding-left: 0;">
      <% @recent_opportunities.each do |opportunity| %>
        <li class='opportunity'>
          <a href='<%= opportunity_path(opportunity) %>'>
            <span><%= opportunity.title %></span>
            <em><%= relative_timestamp(opportunity.posted_at) %></em>
          </a>
        </li>
      <% end %>
    </ul>

     <p class="recent_opps_more"><a href='<%= opportunities_path %>' class='button_uppercase'><%= t('.see_more') %></a></p>
   </div>
<% end %>
